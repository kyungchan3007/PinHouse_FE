import { ListingContentsCards } from "./listingsContentsCards";

export const ListingContentsList = () => {
  return (
    <div className="flex h-full flex-col overflow-y-auto">
      <ListingContentsCards />
    </div>
  );
};
